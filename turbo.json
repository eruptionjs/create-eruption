{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [
        "lib/dist/**"
      ]
    },
    "test": {
      "outputs": []
    },
    "coverage": {
      "outputs": [
        "lib/coverage/**"
      ]
    }
  }
}
